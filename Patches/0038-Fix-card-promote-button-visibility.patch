From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Nuutti <snuutti@protonmail.com>
Date: Mon, 25 May 2026 18:50:24 +0300
Subject: [PATCH] Fix card promote button visibility


diff --git a/Assets/Scripts/Assembly-CSharp/PromotionTrackItem.cs b/Assets/Scripts/Assembly-CSharp/PromotionTrackItem.cs
index 340834d8..fa056938 100644
--- a/Assets/Scripts/Assembly-CSharp/PromotionTrackItem.cs
+++ b/Assets/Scripts/Assembly-CSharp/PromotionTrackItem.cs
@@ -93,3 +93,9 @@ public class PromotionTrackItem : MonoBehaviour
 		bool flag = card.IsReadyToRankUp();
-		SetData(state, title, stringBuilder.ToString(), flag);
+		var canAfford = false;
+		if (state == State.Next && flag)
+		{
+			var cardXpEntry = template.type != CardType.Titan ? currentRuleset.pilotXpRanksRuleset.GetRank(num) : currentRuleset.titanXpRanksRuleset.GetRank(num);
+			canAfford = AccountManager.profile.details.Credits >= cardXpEntry.FusionCost;
+		}
+		SetData(state, title, stringBuilder.ToString(), flag, canAfford);
 		CardXPRanks ranksRulset = card.GetRanksRulset();
@@ -111,3 +117,3 @@ public class PromotionTrackItem : MonoBehaviour
 
-	private void SetData(State state, string title, string items, bool readyToPromote)
+	private void SetData(State state, string title, string items, bool readyToPromote, bool canAfford)
 	{
@@ -118,3 +124,3 @@ public class PromotionTrackItem : MonoBehaviour
 		bool flag2 = state == State.Current && readyToPromote;
-		PromoteButton.gameObject.SetActive(flag);
+		PromoteButton.gameObject.SetActive(flag && canAfford);
 		if (!activeSelf && flag)
